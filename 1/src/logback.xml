<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 1. 配置控制台日志输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">

        <!-- 输出流对象 默认System.out 可改为System.err -->
        <target>System.out</target>

        <encoder>
            <!-- 规定输出格式：%d代表日期 %level代表级别 %c代表类名 %thread代表线程 %msg代表日志内容 %n代表换行
            %-5level 级别从左显示5个字符宽度
            -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}  [%-5level] %c [%thread] : %msg%n</pattern>
        </encoder>

    </appender>

    <!-- 2. 配置file日志输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <encoder>
            <!-- 规定输出格式：%d代表日期 %level代表级别 %c代表类名 %thread代表线程 %msg代表日志内容 %n代表换行
           %-5level 级别从左显示5个字符宽度
           -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}  [%-5level] %c [%thread] : %msg%n</pattern>
            <!-- 文件字符编码 -->
            <charset>UTF-8</charset>
        </encoder>

        <!--输出路径 如果用相对路径需要从项目根目录开始算-->
        <file>1/log/app.log</file>

        <!--指定拆分和压缩规则-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 指定拆分文件名称 %i为拆分索引-->
            <fileNamePattern>1/log/app-%d{yyyy-MM-dd}.log%i.gz</fileNamePattern>
            <!-- 指定拆分文件最大大小 -->
            <maxFileSize>1MB</maxFileSize>
        </rollingPolicy>

    </appender>



    <!-- 2. 设置全局日志输出-->
    <!-- 级别由低到高：TRACE < DEBUG < INFO < WARN < ERROR，通常开发设置为 INFO 或 DEBUG -->
    <!-- 日志级别设置为info ，即输出info级别及以上内容 -->
    <root level="INFO"><!-- 日志开启ALL 取消日志OFF -->
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
